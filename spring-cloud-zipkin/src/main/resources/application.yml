server:
 port: 9000
spring:
 application:
  name: zipkin-server
eureka:
 client:
  serviceUrl:
   defaultZone: http://zz:123456@localhost:2222/eureka/ #注册服务器地址
management:
 security:
  enabled: false #关闭验证
info: #/info请求的显示信息
 app:
  name: ${spring.application.name}
  version: 1.0.0
 build:
  artifactId: @project.artifactId@
  version: @project.version@